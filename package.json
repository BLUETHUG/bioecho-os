{
  "name": "bioecho-web",
  "version": "0.1.0",
  "private": true,
  "description": "BioEcho OS Web App — Biological Intelligence Platform",
  "scripts": {
    "start": "npx serve . -p 8080 --cors",
    "dev": "npx live-server --port=8080 --no-browser"
  }
}
